{
  "installCommand": "npm install --legacy-peer-deps",
  "buildCommand": "npm run build",
  "crons": [
    {
      "path": "/api/cron/check-endpoints",
      "schedule": "0 1 * * *"
    }
  ],
  "functions": {
    "api/cron/check-endpoints.ts": {
      "maxDuration": 30,
      "installCommand": "npm install --legacy-peer-deps"
    }
  }
}